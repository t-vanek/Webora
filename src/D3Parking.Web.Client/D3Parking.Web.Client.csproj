<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <NoDefaultLaunchSettingsFile>true</NoDefaultLaunchSettingsFile>
    <StaticWebAssetProjectMode>Default</StaticWebAssetProjectMode>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
    <!-- Program.cs applies the culture-cookie language at startup. With sharded ICU the runtime
         only carries data for the culture it booted with and hard-fails on any later change
         ("Blazor detected a change in the application's culture"), taking the whole island down
         behind the error overlay. Full globalization data makes the cookie culture always safe. -->
    <BlazorWebAssemblyLoadAllGlobalizationData>true</BlazorWebAssemblyLoadAllGlobalizationData>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.Authorization" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.Authentication" />
    <PackageReference Include="Microsoft.AspNetCore.SignalR.Client" />
    <PackageReference Include="Microsoft.Extensions.Localization" />
    <PackageReference Include="Microsoft.FluentUI.AspNetCore.Components" />
    <!-- The icon package is deliberately NOT referenced here. It is ~22 MB of assemblies that the
         .NET WASM runtime reads on every page load, and this project's single component needs two
         glyphs — those are inlined as SVG in NotificationBell.razor. The server project still
         references it; there it costs nothing to send. -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\D3Parking.Contracts\D3Parking.Contracts.csproj" />
  </ItemGroup>

</Project>
