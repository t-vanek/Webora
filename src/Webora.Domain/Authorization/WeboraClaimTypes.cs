namespace Webora.Domain.Authorization;

public static class WeboraClaimTypes
{
    /// <summary>Claim type carrying a single fine-grained permission (e.g. "Pages.Edit").</summary>
    public const string Permission = "webora:permission";
}
